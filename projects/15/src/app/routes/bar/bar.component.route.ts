import { Routes } from '@angular/router';
import { BarComponent } from './bar.component';

const routes: Routes = [
    {
        path: '',
        component: BarComponent,
    },
];

export { routes };
